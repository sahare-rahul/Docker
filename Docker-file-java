# Use an official OpenJDK runtime as a base image
FROM openjdk:11

# Set the working directory inside the container
WORKDIR /usr/src/app

# Copy the executable JAR file into the container at the working directory
COPY target/app.jar .                               # here  app.jar is our autualy file for app present in target directory

# Expose the port on which your Java application will run
EXPOSE 8080

# Define the command to run your application
CMD ["java", "-jar", "app.jar"]            # and here we run our app.jar    
